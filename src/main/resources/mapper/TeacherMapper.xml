<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hjy.mapper.TeacherMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.hjy.pojo.Teacher">
        <id column="teacher_id" property="teacherId" />
        <result column="teacher_number" property="teacherNumber" />
        <result column="teacher_name" property="teacherName" />
        <result column="password" property="password" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        teacher_id, teacher_number, teacher_name, password
    </sql>
<!--auto generated by MybatisCodeHelper on 2022-04-25-->
    <select id="selectByTeacherIdAndPassword" resultMap="BaseResultMap">
        select *
        from teacher
        where teacher_number=#{teacherId} and `password`=#{password}
    </select>

</mapper>
